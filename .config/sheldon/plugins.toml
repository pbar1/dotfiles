# `sheldon` configuration file
# ----------------------------
#
# You can modify this file directly or you can use one of the following
# `sheldon` commands which are provided to assist in editing the config file:
#
# - `sheldon add` to add a new plugin to the config file
# - `sheldon edit` to open up the config file in the default editor
# - `sheldon remove` to remove a plugin from the config file
#
# See the documentation for more https://github.com/rossmacarthur/sheldon#readme

shell = "zsh"

[templates]
defer = { value = 'zsh-defer source "{{ file }}"', each = true }

[plugins]

# Plugins are sourced in order, thus the order here matters

[plugins.zsh-defer]
github = "romkatv/zsh-defer"

[plugins.base16-shell]
github = "fnune/base16-shell"

[plugins.vanilli-sh]
github = "yous/vanilli.sh"

[plugins.fzf]
github = "junegunn/fzf"
use = ["shell/completion.zsh", "shell/key-bindings.zsh"]

[plugins.fzf-tab]
github = "Aloxaf/fzf-tab"
use = ["{{ name }}.zsh"]

[plugins.go-task-completions]
github = "sawadashota/go-task-completions"
use = ["_task"]
apply = ["fpath"]

# [plugins.docker-completions]
# github = "docker/cli"
# use = ["contrib/completion/zsh/_docker"]
# apply = ["fpath"]

[plugins.zsh-completions]
github = "zsh-users/zsh-completions"
# apply = ["fpath"]

[plugins.zsh-abbr]
github = "olets/zsh-abbr"
use = ["{{ name }}.zsh"]
# apply = ["defer"]

[plugins.zsh-autosuggestions]
github = "zsh-users/zsh-autosuggestions"
use = ["{{ name }}.zsh"]
# apply = ["defer"]

[plugins.zsh-syntax-highlighting]
github = "zsh-users/zsh-syntax-highlighting"
use = ["{{ name }}.zsh"]
# apply = ["defer"]

[plugins.compinit]
inline = 'autoload -Uz compinit && compinit'
