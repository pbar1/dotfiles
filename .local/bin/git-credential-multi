#!/usr/bin/env sh
set -e

current_os="$(uname)"
if [ "${current_os}" = 'Darwin' ]; then
  git credential-osxkeychain "${@}"
elif [ "${current_os}" = 'Linux' ]; then
  git credential-libsecret "${@}"
else
  >&2 echo "no credential helper for os: ${current_os}"
  exit 1
fi
